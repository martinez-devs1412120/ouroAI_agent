"""api/routers — FastAPI routers (route groups).

One module per logical area. Each router is mounted in api/main.py
with a URL prefix:
    api/routers/health.py      -> /health
    api/routers/studyrag.py    -> /ask, /documents  (future)
"""
