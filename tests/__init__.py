"""tests/ — standard library unittest, no pytest required.

Run with:    .venv/Scripts/python.exe -m unittest discover tests
Or directly: .venv/Scripts/python.exe tests/test_calculator.py

We use unittest instead of pytest so the test suite runs with zero
extra dependencies. For a portfolio project, that means anyone cloning
the repo can verify it with the same Python they already have."""
